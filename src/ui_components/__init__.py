"""
SIRIUS LOCAL AI – UI Components Package
---------------------------------------
This package contains the modular UI components used by the SIRIUS UI
subsystem.

The UI components subsystem provides:
- reusable UI elements (panels, toolbars, windows)
- pixel‑level layout rendering
- timeline UI integration
- animation support via the animations engine
- event handling and interactive controls
- integration with the UI manager and runtime engine

Modules inside this package are dynamically loaded by the UI engine.
No imports are performed here to avoid side‑effects during initialization.
"""

