# 🌌 SIRIUS LOCAL AI — Unified Autonomous Workstation Overview  
**Status:** ✔ Active (Enhanced)  
**Version:** 5.9.0 UNIFIED  
**Architecture:** Modular, Deterministic, Explainable, Single-Process Orchestrated, Multi-Alias Persistent, Autonomy‑Supervised  
**Scope:** PC + Workstation Offline AI Operating Environment (Port 8080)  
**Author:** Richard Pizem  

---

## 🎯 Purpose  
SIRIUS Local AI is a fully offline, modular, deterministic autonomous workstation designed to operate Windows 11 and mobile environments safely, intelligently, and explainably.  
It combines symbolic multi-hop reasoning, compound noun phrase preservation (`InputParser5`), autonomous encyclopedic disambiguation triage (`EnvoyExecutionLayer5`), contextual domain shielding (`EnvoyNormalizer5`), multi-alias graph persistence (`autosave_kg.json`), a decoupled 4-Panel UI Suite, centralized execution via `sirius_orchestrator.py` on local port 8080, interactive `PanelAPI` confirmation loops (`[ÁNO/NIE]`), real-time hardware/security supervision via `TimeCore` and `Guard`, supervised autonomy with zero proposal recurrence, and customs-grade enterprise validation (COLNIK‑6.x).

SIRIUS is built to **parse, think, deduce, disambiguate, shield, persist, explain, validate, repair, and execute** — with absolute zero cloud dependency.

---

## 🧩 Core Architecture  
SIRIUS is composed of tightly integrated, decoupled subsystems operating within a unified single-process daemon:

- **Single-Process Orchestrator (`sirius_orchestrator.py`)** — central execution daemon hosting HTTP/WebSocket IPC natively on port 8080  
- **Multi-Word Semantic Parser (`InputParser5`)** — extraction and preservation of compound noun phrases (`ovcia vlna`, `mobilny telefon`) and strict copula verb separation (`je`, `sú`)  
- **Autonomous ENVOY 5 & Disambiguation Triage** — encyclopedic disambiguation resolution, Anti-Prefix Guard, Strip-Bracket Fallback, and Non-Bio Domain Shield (`EnvoyNormalizer5`)  
- **PanelAPI & TimeCore/Guard** — interactive non-blocking `[ÁNO/NIE]` confirmation loops and real-time CPU, RAM, Disk hardware telemetry  
- **KG ENGINE 6.x & Multi-Alias Core** — unified symbolic knowledge graph with dual-key mapping and atomic persistence to `autosave_kg.json`  
- **Zero Proposal Recurrence Engine** — permanent elimination of redundant learning proposals once entities or aliases are confirmed  
- **ReasoningEngine 5.9.0** — deterministic multi‑hop symbolic inference, orbital jumps, and property inheritance  
- **COLNIK‑6.x Customs Decision Gate** — primitive ALLOW / DENY / TRIAGE customs validation across Standard and High-Performance IPC modes  
- **AUTONOMY 6.x (Control, Guard & Triage Mode)** — supervised proposal generation, hardware anomaly containment, and triage routing in `COLNIK-6.x/triage`  
- **EXECUTE 6.x** — deterministic mutation executor and atomic disk committer  
- **4-Panel UI Suite (Port 8080)** — browser dashboard (`Duplicates`, `Triage`, `Navigation`, `Terminal`) with automatic module context reset (`currentModule = "none"`)  
- **Security Family 5.x & Identity Engine 3.1** — constant-time O(1) access tiers (OWNER / FAMILY / STRANGER) and academic bypass  
- **System Agent 5** — hardened OS‑level gatekeeper enforcing reversibility and terminal isolation  
- **Workflow Engine 5.9.0** — deterministic multi-step state machine with proof-tree dispatch  
- **Self‑Repair Layer 5.8** — SHA-256 cryptographic hash sealing and baseline shadow restoration  

Each module is strictly bounded, fully inspectable, explainable, and validated through COLNIK‑6.x.

---

## 🧠 Intelligence Model  
SIRIUS operates on a hybrid symbolic intelligence framework:

### **Symbolic & Semantic Intelligence**
- compound noun phrase preservation without token mutilation (`InputParser5`)  
- Knowledge Graph multi-alias dual-key mapping  
- deterministic relation and property inheritance (`DedicsnostVlastnostiRule`)  
- transitive equivalence chaining (`TranzitivneRelacieRule`)  
- orbital category leaping (`MultiHopOrbitInferenceRule`)  
- automatic structural typing (`AutoTypeInferenceRule`)  

### **Autonomy & Supervisory Intelligence**
- supervised autonomy with active human-in-the-loop oversight  
- zero proposal recurrence on stored concepts  
- single-process orchestration loop (`sirius_orchestrator.py` on port 8080)  
- interactive human gating via `PanelAPI` (`[ÁNO/NIE]`)  
- autonomous Wikipedia disambiguation triage (*„môže byť...“*)  
- Anti-Prefix Guard eliminating semantic query drift (*Káva* -> *Kavala*)  
- safe fallback logic and terminal state release  

### **System & Telemetry Intelligence**
- real-time host hardware auditing via Guard (CPU, RAM, Disk) with less than 1% overhead  
- Non-Bio Domain Shield preventing false habitat properties on technical concepts  
- Sentence-Bound extractor requiring explicit occurrence verbs (*žije*, *obýva*)  
- execution loop anomaly detection and automated throttling  
- quarantined containment in `COLNIK-6.x/triage`  

---

## 🔐 Safety & Customs Validation  
SIRIUS delivers verifiable enterprise safety:

- **COLNIK-6.x Customs Clearance:** all mutations and workflows clear a primitive ALLOW / DENY / TRIAGE decision matrix  
- **AUTONOMY 6.x Supervision:** autonomous proposals mandate human confirmation for novel concepts, while suppressing recurrence for verified ones  
- **Terminal Input Decoupling:** clearing command input immediately resets `currentModule = "none"`, permanently preventing conversational queries from executing as host OS shell commands  
- **Reversibility Guarantee:** destructive disk operations are barred; duplicate files enforce a strict `REPORT_ONLY` policy  
- **Identity Enforcement:** constant-time evaluation of OWNER, FAMILY, and STRANGER profiles, with guaranteed academic bypass for schoolwork  
- **Cryptographic Integrity:** continuous SHA-256 hash checks and shadow restoration via Self-Repair Layer 5.8  

---

## 🖥 4-Panel UI Suite & OS Automation (Port 8080)  
Accessible via browser on `http://127.0.0.1:8080`, the interface isolates management tasks into four dedicated panels:

- **Duplicates Panel:** Displays duplicate file status, monitors Guard hardware metrics, and enforces non-destructive scanning.  
- **Triage Panel:** Real-time visual queue of all quarantined files, unparsed web payloads, and held mutations (`COLNIK-6.x/triage`).  
- **Navigation Panel:** Deterministic, state-safe switching across Runtime Core, Knowledge Graph, Envoy, and Autonomy modules.  
- **Terminal Panel:** Decoupled CLI console with automatic context clearance, ensuring conversational text never captures host terminal focus.  

Host automation operations mediate through System Agent 5 with strict sandbox isolation and verified reversibility.

---

## 📚 Multi-Alias Knowledge Graph  
The unified Knowledge Graph provides:

- compound entities and multi-word concepts natively indexed  
- dual-key alias mapping (colloquial query <-> formal encyclopedic title)  
- atomic persistence committed to `autosave_kg.json`  
- cycle-safe bounded traversal and pathfinding (`KG_RELATE`)  
- developer comfort commands (`kg add entity`, `kg add alias`, `kg list`, `kg release`)  
- Non-Bio Domain Shielding preventing categorical ontological pollution  

---

## 🔍 Deep Explainability (XAI)  
Every deduction and system decision in SIRIUS is verifiable:

- `KG_EXPLAIN` direct relation and property justification  
- `KG_EXPLAIN_DEEP` hierarchical proof trees (rendered in ASCII and HTML)  
- rule derivation provenance and confidence scoring  
- autonomy proposal evidence trees  
- COLNIK customs validation audit trails  

Explainability is mandatory across all execution layers.

---

## ⚙ Deterministic Workflow Pipeline  
User / Web UI (Port 8080)
  |
  v
InputParser5 (Compound phrase preservation & copula verb separation)
  |
  v
sirius_orchestrator.py (Single-Process Daemon on Port 8080)
  |-- 4-Panel UI Suite (Duplicates, Triage, Navigation, Terminal)
  |-- TimeCore & Guard (Temporal tracking & Hardware resource metrics)
  |-- RuntimeCore 5.9.0 & Multi-Alias KG (autosave_kg.json)
  |-- ReasoningEngine 5.9.0 (Multi-hop rules & Proof trees)
  |-- COLNIK-6.x Customs Decision Gate (Standard & High-Performance IPC Mode)
  |-- AUTONOMY 6.x (Control, Guard & Triage Mode in COLNIK-6.x/triage)
  |-- PanelAPI ([ANO/NIE] Confirmation Loop — Zero Recurrence)
  +-- EXECUTE 6.x (Atomic Persistence & Safe Execution)

---

## 📱 Cross‑Platform Core  
SIRIUS executes deterministically across:

- Windows 11 workstations (native execution)  
- Android / Mobile runtimes (via unified agent core)  
- identical Knowledge Graph schemas (`autosave_kg.json`)  
- identical symbolic reasoning rules and explainability proof trees  
- unified identity and academic bypass logic  

---

## 🏁 Summary  
SIRIUS Local AI is a **System‑Intelligent Autonomous Workstation** — a unified, deterministic, explainable, single-process orchestrated, multi-alias persistent offline AI system engineered to operate Windows 11 safely, linguistically intact, and transparently.

It is:

- autonomous with active human oversight  
- linguistically accurate for compound natural language  
- persistent without proposal recurrence  
- decoupled and safe from terminal shell hijacking  
- explainable via hierarchical proof trees  
- validated by customs-grade decision gates  
- self-healing and hash-sealed  
- identity-aware with academic priority  
- 100% offline and sovereign  

A complete, auditable, and self-contained alternative to probabilistic cloud AI systems.
