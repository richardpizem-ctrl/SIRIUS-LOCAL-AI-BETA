# Triage Folder Rules (PRE-FINAL)
# Defines required folder structure for COLNIK operations.

TRIAGE_FOLDER_RULES = {
    "required": [
        "logs",
        "exports",
        "workflow",
        "kg",
        "envoy"
    ],
    "optional": [
        "backup",
        "simulator",
        "temp"
    ]
}

