# Terminal Assistant Rules (PRE-FINAL)
# Defines allowed and forbidden terminal commands for COLNIK.

TERMINAL_ASSISTANT_RULES = {
    "allowed": [
        "help",
        "status",
        "scan",
        "validate",
        "detect"
    ],
    "forbidden": [
        "delete",
        "format",
        "shutdown",
        "kg_wipe",
        "unsafe_exec"
    ]
}

